// ROYAL Frontend runtime config
// When BACKEND_BASE_URL is set, we will progressively switch store calls to real APIs.
// For now, keep it empty to use local store behavior.
window.ROYAL_CONFIG = {
  BACKEND_BASE_URL: "", // e.g. "https://api.yourdomain.com"
};
